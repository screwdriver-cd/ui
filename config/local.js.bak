// sudo vim /etc/hosts
// 127.0.0.1       screwdriver.ouroath.com
// sudo ember serve --ssl true --port 443

let SDAPI_HOSTNAME;
let SDSTORE_HOSTNAME;

SDAPI_HOSTNAME = 'https://api.screwdriver.cd';
SDSTORE_HOSTNAME = 'https://store.screwdriver.cd';

SDAPI_HOSTNAME = 'https://api.screwdriver.ouroath.com';
SDSTORE_HOSTNAME = 'https://store.screwdriver.ouroath.com';

// running localhost
SDAPI_HOSTNAME = 'http://localhost:9001';
SDSTORE_HOSTNAME = 'http://localhost:9002';

// running locallhost with external domain
// SDAPI_HOSTNAME = 'http://172.131.219.232:9001';
// SDSTORE_HOSTNAME = 'http://172.131.219.232:9002';

module.exports = {
  SDAPI_HOSTNAME,
  SDSTORE_HOSTNAME
};
