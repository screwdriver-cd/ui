{{page-title "Pipeline"}}
<div class="pipeline-page-contents">
  <Pipeline::Nav />

  <Pipeline::Header
    @pipeline={{this.pipeline}}
    @collections={{this.collections}}
  />

  <div class="pipeline-main-content {{if this.isEventsPullsJobsRoute "grid"}} {{if this.isExpanded "expanded"}}">
    {{outlet}}
  </div>
</div>
