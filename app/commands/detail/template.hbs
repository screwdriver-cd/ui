{{info-message message=this.errorMessage type="warning" icon="exclamation-triangle"}}

{{command-header
  command=this.versionCommand
  trusted=this.trusted
  isAdmin=this.isAdmin
  onUpdateTrust=(action "updateTrust")
  onRemoveCommand=(action "removeCommand")
}}

{{command-format command=this.versionCommand}}

{{command-versions commands=this.commands}}

{{outlet}}
