<InfoMessage
  @message={{errorMessage}}
  @type="warning"
  @icon="exclamation-triangle"
/>

<CommandHeader
  @command={{versionCommand}}
  @trusted={{trusted}}
  @isAdmin={{isAdmin}}
  @onUpdateTrust={{action "updateTrust"}}
  @onRemoveCommand={{action "removeCommand"}}
/>

<CommandFormat @command={{versionCommand}} />

<CommandVersions @commands={{commands}} />

{{outlet}}