<TcCollectionList @model={{this.model}} @filteringNamespace={{this.targetNamespace}} @collectionType="Commands">
  Commands share binaries (or scripts) across multiple jobs.
</TcCollectionList>

{{outlet}}
