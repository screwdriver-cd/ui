<div class="actions-cell">
  {{#if deleteAllowed}}
    <span {{action "showDeleteConfirmation"}}>
      {{fa-icon "trash" fixedWidth=true title="Delete pipeline"}}
    </span>
  {{/if}}
  {{#if showDeleteConfirmation}}
    {{#bs-modal-simple
      title="Confirm deletion"
      closeTitle="Cancel"
      submitTitle="Confirm"
      size=null
      fade=false
      onSubmit=(action "deletePipeline" pipelineToDelete)
      onHide=(action "cancelDelete")
    }}
      You're about to delete the pipeline "{{pipeline.appId}}". This action cannot be undone. Are you sure?
    {{/bs-modal-simple}}
  {{/if}}
</div>
