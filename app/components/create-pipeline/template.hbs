<div class="left">
  <div class="back">
    {{svg-jar "arrowhead-left" class="img"}}
    <span onclick={{action (mut this.showCreatePipeline) false}}>
      Back
    </span>
  </div>
</div>

<div class="center {{if this.showQuickStartGuide "menu-open"}}">
  <PipelineCreateForm
    @isSaving={{this.isSaving}}
    @prUrl={{this.prUrl}}
    @errorMessage={{this.errorMessage}}
    @onCreatePipeline={{action "createPipeline"}}
    @templates={{this.templates}}
  />
</div>

<div class="right {{if this.showQuickStartGuide "menu-open"}}">
  <QuickstartGuide @isOpen={{true}} />
  {{!-- <QuickstartGuide @isOpen={{showQuickStartGuide}} /> --}}
</div>