import Component from '@ember/component';

export default Component.extend({
	// actions: {
	// 	changeRoute(routeName, args, model) {
	// 		this.modelFor(model).reload();
	// 		let change = this.get('transition');
	// 		change(routeName, args);
	// 	}
	// }
});
