<div>
  {{#each this.crumbs as |crumb index|}}
    {{#if (eq index 0)}}
      <LinkTo @route={{crumb.route}} >
        {{crumb.name}}
      </LinkTo>
    {{else}}
      {{#if (eq crumb this.crumbs.lastObject)}}
        / {{crumb.name}}
      {{else}}
        /
        <LinkTo @route={{crumb.route}} @model={{crumb.name}} >
          {{crumb.name}}
        </LinkTo>
      {{/if}}
    {{/if}}
  {{/each}}
</div>
