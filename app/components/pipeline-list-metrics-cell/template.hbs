<div class={{this.styleNamespace}} ...attributes>
  <div>
    {{#if (not-eq this.jobId "") }}
      {{#link-to "pipeline.metrics" (query-params jobId=this.jobId)}}
        {{fa-icon "line-chart" fixedWidth=true title="Job metrics"}}
      {{/link-to}}
    {{/if}}
  </div>
</div>