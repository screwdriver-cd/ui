{{#tc-collection-list
  model=model
  targetNamespace=targetNamespace
  collectionType="Templates"
}}
  Templates are snippets of predefined code that people can use to replace a job definition in a screwdriver.yaml.<br/>A template contains a series of predefined steps along with a selected Docker image.
{{/tc-collection-list}}

{{outlet}}
