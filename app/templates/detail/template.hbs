{{template-header
  template=versionTemplate
  onRemoveTemplate=(action "removeTemplate")
}}

<h4>Contents:</h4>
{{validator-job name=versionTemplate.name job=versionTemplate.config collapsible=false}}

{{template-versions templates=templates changeVersion=(action "changeVersion")}}

{{outlet}}
