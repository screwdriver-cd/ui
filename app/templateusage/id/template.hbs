<br>
<h1>
{{#if (await this.hasUsers)}} P{{else}} No p{{/if}}ipelines using {{await this.templateName}}/{{await this.templateNamespace}}@{{await this.templateVersion}}</h1>

<TemplateVersionPipelineUsage
  @template={{this.thisTemplate}}
  @pipelineIds={{this.pipelineIds}}
  @pipelines={{this.pipelines}}
/>

{{outlet}}